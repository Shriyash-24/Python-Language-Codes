# We can use CSS as well.
# CSS transforms plain HTML into something prettier.
# In our app name, we have to make new directory named "static".. only this should be the name.
# load tag loads extra functionality.
# we put in allpost.html - {% load static %}

# Added a completely new variable for static files IN SETTINGS.PY
'''
STATICFILES_DIRS = [
    BASE_DIR / "static"
]
'''
