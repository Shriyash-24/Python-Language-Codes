# asgi, wsgi files are used to deploy the django project to the public.
# But as long as we are developing our application, we require testing.
# For testing purpose, django has a built-in development server. It makes testing part easy.
# We will need to run-specific command.
# To run any commands, we need to use command prompt.
# python manage.py runserver (in directory of project)

# If we quit by Ctrl + C then we can't access the local website.
